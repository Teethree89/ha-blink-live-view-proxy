name: "Blink Liveview Proxy"
description: >-
  Local MPEG-TS live-view proxy and clip archive for Blink cameras.
  Install the matching integration via HACS (or from custom_components/ in this repo).
version: "0.1.0"
slug: "blink_liveview_proxy"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
startup: services
boot: auto
options:
  blink_username: ""
  blink_password: ""
  blink_2fa_code: ""
  port: 8088
  cameras: []
schema:
  blink_username: str
  blink_password: str
  blink_2fa_code: str
  port: int
  cameras:
    - slug: str
      entity_id: str
      name: str?
      id: int?
      serial: str?
ports:
  8088/tcp: null
ports_description:
  8088/tcp: "Blink Liveview Proxy HTTP API"
# hassio_api lets the proxy read the current add-on options while it is
# waiting for a 2FA code; /data/options.json is only written at start.
# share:rw is the fallback for anyone who would rather drop the code in
# a file.
hassio_api: true
map:
  - data:rw
  - share:rw
url: "https://github.com/Teethree89/ha-blink-live-view-proxy"
